# Backend API URL. No port for cloud hosts (Render uses HTTPS on 443).
# - Production: https://your-app.onrender.com (no :8000!)
# - Local backend: http://127.0.0.1:8000
# - Leave unset for same-network (uses hostname:8000)
# VITE_API_URL=
